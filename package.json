{
  "name": "redmonitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js && node scripts/validate-data.js",
    "test:browser": "playwright test",
    "publish:report": "node scripts/publish-report.js",
    "feedback:process": "bash scripts/process-feedback-local.sh"
  },
  "dependencies": {
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^7.1.5"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
